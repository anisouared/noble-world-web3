name: Noble World Backend Tests

on:
  push:
    paths:
      - "backend/**"
    branches: ["develop"]
  pull_request:
    paths:
      - "backend/**"
    branches: ["develop"]

jobs:
  test:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: ./backend

    steps:
      - uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: "18.x"
          cache: "npm"
          cache-dependency-path: "./backend/package-lock.json"

      - name: Install dependencies
        run: npm ci

      - name: Run Hardhat tests
        run: npx hardhat test --network hardhat
        env:
          PRIVATE_KEY_LOCALHOST_HARDHAT: ${{ secrets.PRIVATE_KEY_LOCALHOST_HARDHAT }}
          PRIVATE_KEY_BASE_SEPOLIA: ${{ secrets.PRIVATE_KEY_BASE_SEPOLIA }}
          PRIVATE_KEY_BASE_HOLESKY: ${{ secrets.PRIVATE_KEY_BASE_HOLESKY }}
          INFURA_SEPOLIA_RPC_URL: ${{ secrets.INFURA_SEPOLIA_RPC_URL }}
          INFURA_HOLESKY_RPC_URL: ${{ secrets.INFURA_HOLESKY_RPC_URL }}
          ETHERSCAN_API_KEY: ${{ secrets.ETHERSCAN_API_KEY }}
